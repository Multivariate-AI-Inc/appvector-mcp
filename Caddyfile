# Replace yourdomain.com with your actual domain
appvector-mcp.example.com {
    reverse_proxy /mcp* appvector-mcp:3000
    
    # Optional: Add basic headers for better compatibility
    header {
        # Enable CORS if needed
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Content-Type, Authorization"
    }
    
    # Optional: Redirect root to MCP endpoint
    redir / /mcp
}

gamma-mcp.example.com {
    reverse_proxy /mcp* gamma-mcp:8000
    
    # Optional: Add basic headers for better compatibility
    header {
        # Enable CORS if needed
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Content-Type, Authorization, API_KEY, API_URL"
    }
    
    # Optional: Redirect root to MCP endpoint
    redir / /mcp
}

semrush-mcp.example.com {
    reverse_proxy /mcp* semrush-mcp:3001
    
    # Optional: Add basic headers for better compatibility
    header {
        # Enable CORS if needed
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Content-Type, Authorization"
    }
    
    # Optional: Redirect root to MCP endpoint
    redir / /mcp
}

mattermost-mcp.example.com {
    reverse_proxy /mcp* mattermost-mcp:3002
    
    # Optional: Add basic headers for better compatibility
    header {
        # Enable CORS if needed
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Content-Type, Authorization"
    }
    
    # Optional: Redirect root to MCP endpoint
    redir / /mcp
}

superset-mcp.example.com {
    reverse_proxy /mcp* superset-mcp:8001
    
    # Optional: Add basic headers for better compatibility
    header {
        # Enable CORS if needed
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Content-Type, Authorization, X-Superset-URL, X-Superset-Username, X-Superset-Password"
    }
    
    # Optional: Redirect root to MCP endpoint
    redir / /mcp
}

